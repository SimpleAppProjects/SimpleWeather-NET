<Project Sdk="MSBuild.Sdk.Extras/3.0.23">

  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;net7.0</TargetFrameworks>
    <TargetFrameworks Condition=" $([MSBuild]::IsOSPlatform('OSX')) And '$(Configuration)' == 'Release'">$(TargetFrameworks);net7.0-ios;net7.0-maccatalyst</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>TimberLog</AssemblyName>
    <RootNamespace>Timber.Log</RootNamespace>
  </PropertyGroup>

</Project>
