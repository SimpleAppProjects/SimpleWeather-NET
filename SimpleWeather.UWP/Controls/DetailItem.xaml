<UserControl
    x:Class="SimpleWeather.UWP.Controls.DetailItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleWeather.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Name="Control"
    d:DesignHeight="150"
    d:DesignWidth="175"
    Width="175"
    Height="150">

    <Border Background="#10FFFFFF" Margin="15" CornerRadius="2.5" BorderThickness="1">
        <Grid RequestedTheme="Dark">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Name="DetailLabel" Text="{x:Bind Details.Label, FallbackValue='Label', Mode=OneWay}" Grid.Row="0"
                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Viewbox Name="WeatherBox" Grid.Row="1" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource WeatherIcon}" Text="{x:Bind Details.Icon, FallbackValue='&#xf00d;', Mode=OneWay}"
                       RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <RotateTransform Angle="{x:Bind Details.IconRotation}" CenterX="0" CenterY="0" />
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Viewbox>
            <TextBlock Name="DetailValue" Grid.Row="2" 
                   Text="{x:Bind Details.Value, FallbackValue=' 50.00 in', Mode=OneWay}"
                   Foreground="White" 
                   FontFamily="/Assets/WeatherIcons/weathericons-regular-webfont.ttf#Weather Icons"
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="WrapWholeWords"/>
        </Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Mobile">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="DetailLabel.FontSize" Value="13"/>
                        <Setter Target="WeatherBox.Height" Value="30"/>
                        <Setter Target="DetailValue.FontSize" Value="13"/>
                        <Setter Target="Control.Height" Value="125"/>
                        <Setter Target="Control.Width" Value="140"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Desktop">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="641"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="DetailLabel.FontSize" Value="15"/>
                        <Setter Target="WeatherBox.Height" Value="40"/>
                        <Setter Target="DetailValue.FontSize" Value="15"/>
                        <Setter Target="Control.Height" Value="150"/>
                        <Setter Target="Control.Width" Value="175"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Border>
</UserControl>
