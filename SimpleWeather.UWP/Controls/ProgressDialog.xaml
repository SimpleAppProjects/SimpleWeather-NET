<UserControl
    x:Class="SimpleWeather.UWP.Controls.ProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleWeather.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    VerticalAlignment="Center"
    HorizontalAlignment="Center">

    <ContentDialog x:Name="Dialog" Background="Transparent" BorderBrush="Transparent">
        <Border VerticalAlignment="Center" Height="100"
            Background="White"
            BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}"
            BorderThickness="1"
            Padding="20,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ProgressRing IsActive="True" Grid.Column="0"
                          Width="50" Height="50" Padding="10" VerticalAlignment="Center"/>
                <TextBlock Text="Loading..." Grid.Column="1"
                       Padding="10" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </ContentDialog>
</UserControl>
